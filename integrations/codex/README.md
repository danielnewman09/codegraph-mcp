# Codex integration

This directory is the Codex-only plugin package. It contains the plugin
manifest, MCP server declaration, Codex skill, and stdio MCP harness. It
consumes the shared runtime in `../../src/core` and never imports the Pi
extension entry point.
